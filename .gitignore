# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# SQLite databases (generated by the server on first run)
server/data.db
server/data.db-shm
server/data.db-wal

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment & Private/Confidential Credentials (STRICTLY LOCAL)
.env
.env.local
.env.*.local
*.local
*PRIVADO*.md
*CREDENTIALS*.md
ACCESO_SERVIDOR_Y_DESPLIEGUE_PRIVADO.md


# Documento privado con accesos al VPS y claves (solo local, nunca al repo)
documentacionyclaves.md
CREDENCIALES*.md

# Archivos de marca subidos desde Ajustes → Marca (propios de cada instalación)
public/branding/*
!public/branding/.gitkeep
public/fonts/uploads/*
!public/fonts/uploads/.gitkeep
