# Frontend Environment Variables
# En desarrollo local: usar /api si se tiene proxy configurado, o la URL directa de la API
VITE_API_URL=/api

# Ejemplo para desarrollo directo en local sin Nginx:
# VITE_API_URL=http://localhost:3001/api
